{{ form_start(form) }}
    {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
        <a class="btn btn-secondary" href="{{ path('main_accueil') }}">Retour à l'accueil</a>
{{ form_end(form) }}
