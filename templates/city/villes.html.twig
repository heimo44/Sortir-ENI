{% extends 'base.html.twig' %}

{% block title %}Hello CityController!{% endblock %}

{% block body %}
    {# templates/city/list.html.twig #}

    <h1>Liste des Villes</h1>

    <ul>
{#        {% for city in cities %}#}
{#            <li>{{ city.name }} - {{ city.region }}</li> #}{##}{# Modifiez les clés selon la structure des données #}
{#        {% endfor %}#}
    </ul>
{% endblock %}
